  <div>

The command that we ran only returned a status to us, but it could have just as easily been a script or install command.  They key is that all operations can be reduced to a command syntax, and that we have a mechanism to remotely execute those commands in parallel.  A little more can be added to clean this up.
  </div>

